<div class="shop-workspace"><div class="page-heading"><div><p class="eyebrow">{{ __('ui.shop') }}</p><h1>{{ __('ui.shop') }}</h1><p class="muted">{{ __('ui.shop_intro') }}</p></div></div><section class="panel"><div class="panel-heading"><h2>{{ __('ui.products') }}</h2><span class="count">{{ $products->count() }}</span></div>@if($products->isEmpty())<p class="muted">{{ __('ui.product_empty') }}</p>@else<table><thead><tr><th>{{ __('ui.title') }}</th><th>{{ __('ui.price_cents') }}</th><th>{{ __('ui.status') }}</th></tr></thead><tbody>@foreach($products as $product)<tr><td>{{ $product->title }}</td><td>{{ $product->price_cents === null ? '—' : number_format($product->price_cents / 100, 2, ',', '.').' '.$product->currency }}</td><td>{{ __('ui.'.$product->status) }}</td></tr>@endforeach</tbody></table>@endif</section><section class="panel form-panel"><h2>{{ __('ui.new_product') }}</h2><form method="post" action="{{ route('shop.store', request()->boolean('embed') ? ['embed'=>1] : []) }}">@csrf<label>{{ __('ui.title') }}<input name="title" required maxlength="255"></label><label>{{ __('ui.description') }}<textarea name="description" rows="3" maxlength="2000"></textarea></label><div class="form-row"><label>{{ __('ui.price_cents') }}<input type="number" name="price_cents" min="0"></label><label>{{ __('ui.currency') }}<input name="currency" value="EUR" maxlength="3" required></label><label>{{ __('ui.status') }}<select name="status"><option value="draft">{{ __('ui.draft') }}</option><option value="active">{{ __('ui.active') }}</option><option value="archived">{{ __('ui.archived') }}</option></select></label></div><button class="button">{{ __('ui.create_product') }}</button></form></section><section class="panel"><div class="panel-heading"><h2>{{ __('ui.sales') }}</h2><span class="count">{{ $sales->count() }}</span></div>@if($sales->isEmpty())<p class="muted">{{ __('ui.sales_empty') }}</p>@else<table><thead><tr><th>{{ __('ui.products') }}</th><th>{{ __('ui.customer') }}</th><th>{{ __('ui.amount') }}</th><th>{{ __('ui.status') }}</th></tr></thead><tbody>@foreach($sales as $sale)<tr><td>{{ $sale->product?->title ?? '—' }}</td><td>{{ $sale->customer_email ?? '—' }}</td><td>{{ number_format($sale->amount_cents / 100, 2, ',', '.').' '.$sale->currency }}</td><td>{{ $sale->status }}</td></tr>@endforeach</tbody></table>@endif</section></div>
