<nav class="public-pagination" aria-label="{{ __('public.pagination') }}">@if($items->previousPageUrl())<a href="{{ $items->previousPageUrl() }}">← {{ __('public.previous') }}</a>@endif @if($items->hasPages())<span>{{ $items->currentPage() }} / {{ $items->lastPage() }}</span>@endif @if($items->nextPageUrl())<a href="{{ $items->nextPageUrl() }}">{{ __('public.next') }} →</a>@endif</nav>
