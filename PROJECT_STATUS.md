# Atapin Media — статус проекта

Актуализировано: 17 сентября 2026 года.

## Реализовано

- `platform/` работает на Laravel 13 / PHP 8.4 как self-hosted single-tenant приложение. Готовы foundation, auth/users/RBAC, encrypted settings, audit, Media Library, imports, проекты, задачи, календарь, publishing, community, analytics, public website и оболочка Media Desktop. Интерфейс построен на Blade и progressive JavaScript, обязательной production Node-сборки нет.
- Media Desktop использует единый native workflow: компактное создание и inline-редактирование проектов, задач, материалов, книг и Podcast без лишних Desktop-окон. Проекты показывают paginated связанные задачи, материалы, книги и внешние публикации; специализированные материалы открываются в соответствующем редакторе.
- Общие кнопки удаления в каталогах корректно работают и в карточках, и в списках: подтверждение не блокируется обработчиком родительской карточки, после успешного `DELETE` каталог обновляется. Исправление распространяется на проекты, темы и книги/PDF.
- Темы и категории стали общей иерархической системой для Beiträge, Videos и Bücher. В Media Desktop редакторы показывают заметный multi-select, а карточка темы позволяет массово искать, фильтровать, добавлять и снимать назначения по разделам с отдельными permission checks. Служебные, архивные, Community, Podcast и Live-записи не смешиваются с редакционными разделами.
- Public Website показывает динамическую деревянную полку на Start и Beiträge: все активные категории и темы включены независимо от публикации, на неё помещается столько целых групп, сколько позволяет ширина; `Alle ansehen` появляется только при переполнении. На desktop Start показывает темы книгами внутри категорий, а при занятости менее половины использует составной фон с глобусом и цветком. На мобильном Start показывает книгами сами категории разной высоты и ведёт к их страницам; количество тем внутри категории не раздувает полку. Открытая тема не повторяется книгой, остальные книги и категории остаются доступны. Книги опираются на деревянную поверхность и при наведении выдвигаются без поворота; названия категорий стоят на передней кромке ниже корешков. Название категории на Start открывает `/themen?category=slug`, на Beiträge фильтрует раздел. `/themen` показывает книжный шкаф по одной полке на активную категорию; выбранная категория использует своё публичное изображение и показывает поверх него только крупное название без плашки и счётчиков, а ниже выводит до 10 новых Beiträge, Videos и Bücher, включая материалы всех дочерних тем. Книга-тема на этой странице переключает все три подборки на тему и сама скрывается с полки; ссылки `Alle anzeigen` открывают соответствующий отфильтрованный каталог. Переходы к остальным категориям остаются на общей полке. Свободная верхняя полка шкафа и свободная обзорная полка используют свисающий цветок, третья свободная полка шкафа — глобус, заполненные полки — пустой фон. Если на Start нет книг-тем, используются два цветка и глобус; пустая полка шкафа — два цветка без глобуса. Свисающая часть абсолютно расположена поверх следующего блока и не меняет высоту полки. Videos/Bücher сохраняют компактную breadcrumb-навигацию; legacy `?tag=` остаётся отдельным metadata-фильтром.
- Мобильная Start-страница использует компактные панели: последние Videos/Beiträge выводятся горизонтальными строками с небольшими изображениями и ограниченными заголовками, длинное описание книги скрывается, рекомендация книги и Live занимают меньше высоты. На мобильной странице Beiträge новые материалы также идут полноширинными компактными строками вместо двух узких колонок; заголовок ограничен тремя строками, excerpt — двумя, а популярные Beiträge используют уменьшенную строку-превью.
- Задачи поддерживают состояния `open/planned/working/waiting/done`, приоритеты, сроки, проект, ответственного, checklist/tags и повторение `once/daily/weekly/monthly/custom`. Повторы вычисляются из одной задачи без создания дубликатов. Доска и список сохранены; календарь показывает задачи, публикации и Live и открывает точную исходную запись.
- Книги/PDF имеют title-first intake, inline editor, queued PDF analysis/generation, private originals, sample/full attachments и Stripe-entitlements. Рецензии перенесены в native Desktop-диалог с фильтрами, пагинацией и approve/reject; legacy `book-admin` UI удалён, старый маршрут безопасно перенаправляет в Bücher & PDF.
- Jodit Editor 4.13.9 размещён локально и используется для `Beschreibung`, `Inhaltsverzeichnis`, текста полной PDF-редакции, длинных текстов Beiträge/Videos/Podcast и страниц `Website & Autor` (Impressum, Datenschutz, редакционные сведения, Über uns, Mission, правила Community). Самодельные rich-text панели заменены. Публичные книги, материалы, homepage book feature и информационные страницы отображают безопасную HTML-разметку вместо буквальных тегов; короткие карточки книг используют plain-text excerpt. PDF-генератор сохраняет разрешённое оформление текста.
- Редакторы Beiträge/Videos и Media Library открывают рабочую форму первой: длинный дубль текста/предпросмотра убран сверху, сведения об источнике и файлах свернуты ниже, селекты собраны в адаптивную сетку, темы остаются у основных полей. `Mit KI einordnen` сохранена с пояснением. У привязанного файла убрана отдельная кнопка `Ersetzen`; выбор существующего материала теперь не загружает полный архив и требует адресный поиск от двух символов.
- Сохранённые импорт-версии показывают дату и удаляются по одной красным крестом после подтверждения. Удаление требует `content.edit` (для опубликованной записи также `content.publish`), записывается в audit и не меняет текущий `SourceRecord` или оригинальные медиафайлы.
- Podcast создаётся и редактируется inline поверх существующих `SourceRecord`, `Media`, assignments/assets. Поддержаны audio/video podcast, cover/file, episode/season, историческая дата публикации и явный переход к Publishing для будущего расписания.
- Live Studio сохраняет отдельные OBS и Browser modes. В Browser Studio владелец явно выбирает один из ещё предстоящих эфиров сегодняшнего дня; автоматически открытая в OBS запись не определяет Browser-эфир. Если таких записей нет, в том же экране открывается быстрое создание: название, необязательные описание и анонс-картинка сохраняются при одном нажатии красной `Live starten` и сразу запускаются. Отдельный календарный шаг и Browser-галочка не нужны; OBS-активация и выбор остались прежними. Компактное локальное превью можно вынести в отдельное окно без пустой колонки; камеру поверх экрана/изображения можно двигать и менять её размер. Устройства, изображение/экран, аудиозапись и трансляция сгруппированы. Во время серверного запуска событие показывает `Verbindung läuft`, а Website получает статус `live` только после готовности реального выходного потока. Ошибка WHIP откатывает публикацию, режим, время и статус к исходным значениям; Browser-старт ограничен 10 попытками в минуту с указанием ожидания.
- Настройки разделены по назначению: KI cover-настройки находятся в `KI → Bilder & Cover`, а `Website & Autor` содержит данные автора и website sayings.
- Загрузчики Media Desktop приведены к общему workflow: файл можно выбрать или перетащить, загрузка начинается сразу через существующий resumable Media Library transport, показывает состояние и передаёт форме готовый `media_id`. Автозагрузка означает только передачу файла на сервер: сохранение записи, привязка к книге/материалу, закрытие или перезагрузка редактора происходят только после явного `Speichern`, `Datei übernehmen`, `Import starten` или другого целевого действия пользователя. Общая drop-zone используется для изображений проектов/категорий, файлов книг и PDF intake, Podcast media/cover, замены вложений, профиля, обоев и фотографии автора; Import и Live сохраняют свои специализированные превью/паузы, но также загружают файл до финального сохранения. Media Library поддерживает множественный выбор и получила тот же визуальный drop-control.
- Stripe получил отдельную Test/Live-конфигурацию, encrypted secrets, точный webhook URL, read-only `/v1/account` check и расширенные Checkout metadata для цифровых PDF. Подписанный webhook, amount/currency checks, refund и entitlement flow сохранены. Физическая доставка не добавлена, потому что текущий каталог продаёт цифровые файлы.
- Meta OAuth реализован через App ID/Secret, state, short-to-long token exchange, обязательные scopes, выбор Facebook Page и связанный Instagram professional account. Page token хранится encrypted один раз; check проверяет app/page permissions и Instagram, disconnect удаляет credentials.
- YouTube, X и Telegram имеют реальные read-only connection checks, понятные состояния, безопасные redacted logs и disconnect. YouTube/X обновляют истёкший access token через refresh flow; Telegram проверяет bot, публичный channel/supergroup, membership и право публикации. Пустые profile-only формы больше не создают фиктивные publishing connections.
- Public Website, protected downloads, newsletter, polls, first-party analytics, imports/Takeout, AI suggestions, community inbox, YouTube comments, Browser Studio, PWA и operational health остаются на существующих production-compatible контрактах.
- `/suche` ищет опубликованные видео, лекции, интервью, Beiträge, Podcast, Live, Community и опросы, а также активные книги по названию/описанию/оглавлению и активные темы/категории. Public KAI получает свежий опубликованный контекст и верхнюю навигацию непосредственно из БД/конфигурации; вопрос Live KAI и интерактивная помощь Desktop отвечают в том же HTTP-запросе без worker. Длинные редакционные предложения KI по-прежнему выполняются в очереди.
- Руководство администратора `docs/KAI-ADMIN-MANUAL.md` содержит действия и пути по разделам со статусами `implemented/planned/unverified`. `kai:sync-knowledge` загружает его в отдельную таблицу, добавляет актуальную public-навигацию и запускается планировщиком каждые шесть часов. Desktop KAI отвечает из этого индекса и свежего публичного контекста; Public KAI также получает только публичные разделы руководства. Помощь Desktop открывается компактной панелью с крестиком и закрытием нажатием вне панели.

## Текущее состояние и решения

- Generic Core остаётся независимым от Manna Vom Himmel; немецкий — язык первого deployment, UI-строки идут через translations.
- Import регистрирует existing original и не публикует автоматически. Защищённые файлы доступны только через authorization/entitlement contracts.
- Будущая публикация Podcast выполняется через Publishing; поле даты в Podcast хранит фактическую/историческую дату, а не создаёт скрытый scheduler.
- OAuth app configuration отделена от подключённого аккаунта. Секреты не возвращаются в браузер и не записываются в открытые settings/logs.
- Публичный контекст KAI ограничен опубликованными записями, активными книгами и темами; приватные полные PDF, черновики и секреты не индексируются. Подпись `planned` означает «предусмотрено, ещё не сделано», `unverified` — наличие кода без подтверждённого production/API результата.
- Taxonomy assignments используют существующие canonical subject types `record` и `product`; в этом блоке их схема не менялась. Добавлена только новая таблица руководства KAI; Composer/npm dependencies не менялись.
- В Media Desktop родитель темы выбирается только из активных категорий. Существующие legacy-связи темы с темой не выводятся как новые варианты и сохраняются при редактировании других полей, пока редактор явно не выбирает категорию либо пустое значение.
- Rich-text HTML при сохранении и публичном выводе проходит общий `RichContent` sanitizer: разрешены редакционные заголовки, списки, цитаты, таблицы, безопасные ссылки и ограниченное выравнивание. Скрипты, event-атрибуты, iframe и произвольные стили не допускаются. Jodit подключён как локальный статический vendor asset с MIT license; Composer/npm dependencies и схема БД не менялись.
- На Videos и Bücher маленькие обложки категорий в компактной taxonomy-навигации выводятся только из публичного canonical image storage; private media-preview URL не попадает в страницу. При отсутствии обложки навигация остаётся текстовой. Для desktop полка выбирает предоставленные владельцем составные PNG 1024 px шириной как CSS-фон, а при узком экране сохраняет пропорции деревянной полки и размещает предметы CSS-фонами без дополнительных HTML-картинок и расчётов в JavaScript. Вместимость групп и условная ссылка пересчитываются при изменении ширины.
- Удаление импорт-версии касается только выбранной строки snapshot. Следующий повторный импорт того же источника может создать эту версию заново; подавление повторного импорта удалённых snapshot не добавлялось без отдельного решения по provenance.
- Отдельное окно Browser Studio показывает беззвучную локальную копию canvas, без второго публичного плеера или отдельной передачи. Кнопка открытия расположена рядом с `Lokale Vorschau` внутри панели; при отдельном окне она остаётся доступной слева возле заголовка студии как `Vorschaufenster zeigen`, а после закрытия возвращается к восстановленному встроенному превью. Поле WebRTC hostname подставляет hostname текущей страницы, если серверное значение пусто, и объясняет требуемый формат. Применение защищённой серверной конфигурации показывает ход, отказ и результат непосредственно возле кнопки; при откате указывает этап отказа. Успешно сохранённая Browser-настройка глобальна для следующих Livestreams.
- Попытки применения защищённой Live-конфигурации записываются в отдельный приватный Laravel-журнал `private/atapin-platform/storage/logs/live-server-configuration.log`: результат, этап, тип ошибки и exit code процесса без конфигурации и секретов. Ошибка записи журнала не меняет результат применения или отката. MediaMTX runtime warnings остаются в отдельном `private/atapin-live/server.log`.
- Проверка кандидата MediaMTX и idempotent запуск из Live Studio оставляют вывод Symfony Process включённым: при `disableOutput()` Symfony открывал `/dev/null`, доступ к которому может запрещать Plesk PHP `open_basedir`. При отказе приватный журнал также сообщает basename/строку источника ошибки и код `dev_null_unavailable`, если warning указывает на этот путь, без сырого сообщения или секретов.
- Browser Studio публикует H.264/Opus через защищённый loopback WHIP сразу в canonical MediaMTX path `live`, который уже использует Website. MediaMTX самостоятельно создаёт HLS и fMP4-запись из поддерживаемых кодеков; промежуточный RTSP path, постоянно падавший FFmpeg relay и второй publish hop удалены. OBS остаётся на том же `live` path через RTMP/RTMPS, а блокировка входа и scoped browser-token не позволяют двум источникам публиковать одновременно.
- Одиночная временная ошибка Browser heartbeat больше не вызывает `stop()` работающего WebRTC-потока: клиент продолжает передачу и повторяет status-запрос в пределах подтверждённой серверной аренды. Только авторитетный отказ сессии/прав либо истечение аренды завершает эфир. Реальный `RTCPeerConnection.failed` теперь показывает отдельную причину с указанием проверить MediaMTX и TCP/UDP 8189 вместо нейтрального статуса завершения.
- Короткий остаток server-side start throttle (до 10 секунд) Browser Studio пережидает и повторяет один раз внутри того же нажатия. Пользователь больше не получает обвиняющее сообщение `Zu viele Startversuche` из-за нескольких секунд скользящего окна; длительная блокировка остаётся видимой без автоматического цикла запросов.
- Завершённый Live появляется в публичном каталоге и блоке `Aufzeichnungen` только после того, как к событию прикреплён реально существующий воспроизводимый MP4/WebM. Один статус `ended`, ожидание обработки или отсутствующий файл больше не создают ложную публичную запись; прямая страница события остаётся доступной с существующим статическим fallback.
- OBS `Live-Monitor` в Media Desktop встраивает непосредственно защищённый MediaMTX player canonical-потока `/_live/live/`, а не публичную страницу `/live` с header, описанием и чатом. Метрики, запись, внешние выходы и Community остаются отдельными Studio-блоками под видеомонитором.
- После завершения OBS пустой shared HLS path остаётся доступным без Basic-auth challenge, пока MediaMTX закрывает player/muxer; при активном неопубликованном эфире чтение по-прежнему запрещено. Публичный плеер параллельно проверяет `/live/current` каждые две секунды и при окончании немедленно выгружает iframe в штатную заставку. Поэтому финальные HLS-запросы больше не превращаются в окно логина браузера и чёрный экран с `Network Error 401`.
- Browser Studio заполняет 16:9 canvas экраном, изображением и камерой через пропорциональное `cover`: синие поля не попадают в отправляемый кадр, лишние края источника аккуратно обрезаются без растяжения. Публичный Live-блок на обзорной странице также имеет 16:9 вместо слишком широкой рамки, поэтому 16:9 OBS/Browser-поток не получает дополнительные чёрные боковые поля. Browser heartbeat имеет отдельный запас лимита, а idempotent `Live beenden` больше не конкурирует с heartbeat за `throttle` и не оставляет серверную сессию активной после локального завершения.
- Единый upload-компонент не создаёт второй файловый transport: он использует прежние chunk/resume endpoints, оптимизирует изображения по назначению и блокирует сохранение формы до окончания фоновой загрузки. Завершение upload не отправляет основную форму и не применяет файл само по себе; редактор остаётся открытым до явного действия пользователя. Серверные формы сохраняют обратную совместимость с обычным multipart upload и дополнительно принимают уже загруженные media IDs.

## Известные ограничения

- Реальные OpenAI, Stripe, SMTP, Meta, YouTube, X, Telegram, OBS/WHIP и production credentials локально не использовались; интеграционные ответы проверены через HTTP fakes и контрактные тесты.
- TikTok и LinkedIn пока остаются profile-only, без фиктивного publishing API.
- Защищённая Live Server конфигурация уже применена владельцем на production и глобально сохраняется; direct-WHIP конфигурацию нужно повторно применить после production-обновления, затем проверить реальный Browser-эфир. `platform:check` и production-проверка direct-WHIP агентом не выполнялись.
- Перед production migration обязателен backup базы данных.
- Новая таблица KAI и первоначальная синхронизация руководства ещё не применялись на production. Реальный OpenAI ответ после этого изменения агентом не проверялся; HTTP fakes и контрактные тесты прошли. Справочник требует обновления при изменении кнопок и маршрутов.
- Вставка изображений непосредственно в rich-text пока отключена: её следует добавить отдельным блоком через защищённую Media Library и проверку public media URLs; arbitrary URL/base64 images не допускаются.
- Полный сквозной Playwright-сценарий приложения для этого блока не запускался; изолированные Edge browser checks Public Website прошли локально, остальные сквозные проверки остаются в CI.
- На production прежняя защищённая конфигурация откатывалась на этапе `validation` с `ErrorException`; Symfony Process при отключённом выводе открывал `/dev/null`, исключённый из Plesk PHP `open_basedir`. После опубликованного исправления владелец применил конфигурацию: Live Studio показала `Serverkonfiguration gespeichert. Diese Einstellung gilt auch für kommende Livestreams.`, а после обновления страницы — `Server-API erreichbar`. Настройка глобальна и не требуется для каждого эфира. Публичный TCP/UDP 8189 открыт владельцем; реальная Browser-трансляция и `platform:check` ещё не подтверждены.

## Проверки

- Полный локальный PHPUnit suite на PHP 8.4 / SQLite с нужными CLI extensions: **477 tests, 3880 assertions — passed**. Локальный `composer test` без включённых расширений не пригоден для этого окружения; PHPUnit запущен напрямую с изолированным PHP ini.
- Изолированный Edge browser check помощи KAI на `1672 × 941` и `390 × 844` прошёл: компактная немодальная панель, расположение, закрытие крестиком/значком/вне панели. `kai:sync-knowledge` создал 60 записей в тестовой SQLite; расписание подтвердило запуск каждые шесть часов.
- Изолированные Chrome browser checks: форма Beiträge/Media Library и сетка селектов на `1672 × 941` и `390 × 844`, положение Jodit/taxonomy/действий с файлами, адресный поиск и индивидуальное удаление импорт-версий — passed. Сквозные проверки обновлены в CI, но локально не прошли по ограничению браузерного localhost.
- PHP/Node syntax затронутых файлов, Blade `view:cache`, routes `route:cache` — passed; generated caches очищены. Composer audit и npm build не требуются: manifest dependencies не менялись.
- Production подтвердил, что временный WebRTC browser-поток поступал в MediaMTX, но оба варианта промежуточного FFmpeg publish hop (RTSP и RTMP/FLV) одинаково завершались после первых сотен кадров. Поэтому этот relay удалён архитектурно. Direct-WHIP покрывает canonical `live` path, scoped авторизацию, откат неудачного WHIP-старта, OBS mutual exclusion, HLS/recording hooks и AAC-нормализацию только для необязательных внешних RTMP-назначений. Целевые **31 PHP test, 235 assertions — passed**; PHP syntax прошёл. Реальный production-эфир после перехода ещё нужно повторить.
- После direct-WHIP production дважды выходил в эфир, но клиент закрывался примерно через 36–41 секунду (1084–1233 закодированных кадра). Код показал, что любая единичная ошибка десятесекундного heartbeat немедленно вызывала локальный `stop()`; этот fail-fast устранён. Целевые **12 PHP tests, 113 assertions**, PHP/Node syntax, Blade `view:cache` и Edge browser-check с первым HTTP `429`, автоматическим повтором, desktop/mobile layout и сохранением эфира при смене вкладки прошли; generated views очищены. Повторный production-тест после client-side исправления ещё требуется.
- Для публикации готовых Live-записей и связанного recording pipeline целевые **44 PHP tests, 318 assertions — passed**; PHP syntax прошёл. Проверены каталог `/live`, блок `Aufzeichnungen`, готовый MP4, исключение завершённых событий без файла, remux и live hooks.
- Для OBS Live Monitor целевые **29 PHP tests, 224 assertions — passed**; Node syntax и Blade `view:cache` прошли, generated views очищены. Отдельно проверено отсутствие публичного `/live?event=…` внутри Studio iframe и использование прямого `/_live/live/?cookieCheck=1`.
- Для безопасного завершения публичного OBS-плеера целевые **44 PHP tests, 335 assertions — passed**; PHP/Node syntax, Blade `view:cache` и routes `route:cache` прошли, generated caches очищены. Проверены HLS read после end-сигнала, запрет для снятого с публикации события, быстрый current-status check и public player markup.
- Для геометрии Browser/OBS и надёжного завершения целевые **54 PHP tests, 445 assertions — passed**; PHP/Node syntax, Blade `view:cache` и routes `route:cache` прошли, generated caches очищены. Проверены `cover` compositor, 16:9 public frame, HLS offline без auth challenge, запрет чтения активного приватного эфира и завершение после десяти heartbeat. Изолированный Playwright layout-check не запускался: локальный Chromium Headless Shell отсутствует.
- Для полноценной страницы выбранной категории целевые **34 PHP tests, 326 assertions — passed**. Проверены публичная обложка категории, запрет private preview URL, подборки Beiträge/Videos/Bücher по категории и её дочерним темам, тематическое переключение с полки и ссылки на полные отфильтрованные каталоги.
- Для упрощённой hero-карточки категории целевые **6 PHP tests, 109 assertions — passed**; Blade templates успешно скомпилированы, generated views очищены. Проверено отсутствие прежней плашки у выбранной категории.
- Для отдельного Live-конфигурационного журнала PHP syntax и Laravel `config:cache` прошли, config cache очищен. Изолированная запись через новый logging channel создала ожидаемую запись с этапом/типом/exit code в локальном runtime log; QA-файл затем удалён. После deployment production-журнал подтвердил откат `validation` с `ErrorException`.
- Для совместимости защищённой Live-конфигурации с Plesk PHP `open_basedir` PHP syntax и локальная проверка вызова Symfony Process прошли. Linux-проверка с исключённым `/dev/null` добавлена в CI; локально не запускалась, поскольку WSL/Linux и pinned MediaMTX здесь отсутствуют. Владелец подтвердил успешное применение на production и доступность Server-API после обновления страницы.
- Для составных CSS-фонов полки целевые **34 PHP tests, 301 assertions — passed**. Edge browser проверил Start, `/themen` и Beiträge на `1672 × 941`, `390 × 844` и `1280` px: составной фон с глобусом/цветком на занятой менее чем наполовину desktop-полке Start, мобильные книги-категории разной высоты вместо книг-тем, условную кнопку `Alle ansehen`, цветок сверху, глобус на третьей свободной полке и его отсутствие на второй, свисание без сдвига следующей полки, расположение названий категорий ниже книг, сохранение книг/категорий и отсутствие horizontal overflow. Отдельный пустой fixture проверил Start с двумя цветками/глобусом и полку шкафа с двумя цветками на desktop/mobile. Полный suite после этого ограниченного UI-блока не повторялся.
- Для компактной мобильной редакционной выдачи Edge browser проверил Start и Beiträge на `390 × 844`, а desktop-регрессию — на `1672 × 941`: размеры изображений, line clamp заголовков/excerpt, скрытие длинного книжного описания, компактный Live, одно-колоночные Beiträge и отсутствие horizontal overflow. Целевые **34 PHP tests, 301 assertions — passed**; Blade cache и Node syntax прошли.
- Для единого загрузчика полные `FoundationTest` и `DesktopWorkspacesTest`: **56 tests, 547 assertions — passed** на PHP 8.4/SQLite; `media-upload-controls.mjs`, синтаксис всех затронутых JS и PHP-файлов, а также `git diff --check` прошли. Отдельная визуальная browser-проверка Desktop не запускалась.
- Для запрета скрытого автосохранения `media-upload-controls.mjs` и Node syntax обоих изменённых JS-файлов прошли; тест фиксирует раздельные стадии upload и явного применения. Целевой PHP-тест и Blade compile локально не запустились: Windows Application Control заблокировал имеющийся `C:\php\php.exe` до старта процесса.

## Что рекомендуется следующим

- Перед production migration сделать backup БД через Plesk; затем получить текущую ветку, применить migration, выполнить `kai:sync-knowledge` и очистить Blade views. Проверить реальный вопрос о книгах/меню/запланированной функции на public и Desktop KAI и быстрый `/suche`. После обновления Live Studio повторить Browser-эфир: в Desktop должен появиться `Verbindung läuft`, затем `Live auf der Website`, а публичная `/live` должна получить плеер; после этого выполнить документированный `platform:check`. На production отдельно проверить глобус и положение названий категорий на Start с реальными данными.
- При следующем запуске CI проверить обновлённые сквозные браузерные сценарии на штатном Linux/Chromium окружении.
- Отдельно спроектировать защищённый выбор изображений из Media Library для Jodit, если изображения в тексте нужны владельцу.
- В настройках по очереди сохранить app credentials и пройти реальные OAuth/API checks Meta, YouTube, X, Telegram и Stripe test mode. Секреты в чат не присылать.
- Выполнить production smoke-test задач/календаря, книг/рецензий, Podcast/Live и `platform:check`.

## Последний связанный commit

- Явное сохранение после автозагрузки: этот commit — `Require explicit save after file upload`. Branch/upstream: `main` → `origin/main`.
- Единый загрузчик Media Desktop: этот commit — `Unify desktop file uploads`. Branch/upstream: `main` → `origin/main`.
- Текущая страница категории: этот commit — `Build complete category content page`. Branch/upstream: `main` → `origin/main`.
- Упрощённая hero-карточка выбранной категории: этот commit — `Show only category title over its image`. Branch/upstream: `main` → `origin/main`.
- Текущий мобильный UI-блок: этот commit — `Compact mobile editorial cards`. Branch/upstream: `main` → `origin/main`.
- Текущий Live Studio follow-up: этот commit — `Keep browser live through transient heartbeats`. Branch/upstream: `main` → `origin/main`.
- Текущая публикация Live-записей: этот commit — `Publish live recordings only when playable`. Branch/upstream: `main` → `origin/main`.
- Текущий OBS Live Monitor: этот commit — `Show only video in OBS live monitor`. Branch/upstream: `main` → `origin/main`.
- Текущее завершение публичного Live-плеера: этот commit — `End public live playback without auth prompts`. Branch/upstream: `main` → `origin/main`.
- Текущая геометрия и завершение Browser/OBS: этот commit — `Fit live video and make stopping reliable`. Branch/upstream: `main` → `origin/main`.
- Предыдущий Live Studio follow-up: `d422dae` — `Publish browser live directly through WHIP`.
- Предыдущий блок полки: `ea23535` — `Adapt home shelf for desktop and mobile`.
