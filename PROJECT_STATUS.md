# Atapin Media — статус проекта

Актуализировано: 14 сентября 2026 года.

## Реализовано

- Public-Website forms for live chat/comments, newsletter, contact, community, reviews and account actions submit through AJAX without document reloads. Live chat clears only its input, shows the sender status and triggers an immediate heartbeat (queued if another heartbeat is already running); JSON responses retain the normal redirect fallback for no-JavaScript clients. Public JS cache version is `v=11`.
- Все девять overview-страниц используют общую рукописную надпись вне featured-медиа: справа на desktop, ниже медиа на ширине ≤1600 px. Главная сохраняет свою фразу; Videos, Beiträge, Bücher, Live, Podcast, Community, Über uns и Mission получили разные короткие христианские тексты de/en. Белое облачко усилено под основным hero-текстом и боковыми надписями, сохраняя мягкое затухание и яркий фон вне текста. CSS cache version обновлена.
- Убраны маленькие декоративные стрелки у шести карточек разделов главной, в общих ссылках заголовков контентных панелей, Bücherregal и заглушках; ссылки сохранены. Стрелки кнопок действий и пагинации не менялись.
- Назначение по длительности в Import Center использует существующие переводы imports.posts/videos/shorts вместо отсутствующих imports.target_*; список показывает Beiträge, Videos, Shorts, а не translation keys.
- Карточка Google Takeout в Import Center содержит только краткую подпись. Подробная инструкция перенесена в существующую справку «?» окна; дублирующий текст убран из выбранного Takeout-панеля. Справка de/en актуализирована для private/Takeout, SHA-256-копирования, прямой выдачи public/media и настроенного правила длительности.
- Media Desktop deeplink `?open=...` выполняется один раз и удаляется из текущего адреса без перезагрузки; остальные параметры и fragment сохраняются. Закрытая Community больше не открывается повторно при reload/повторном входе; сохранённые открытые окна и новые явные deeplinks продолжают работать. Версия desktop-os.js обновлена для browser cache.
- Основа: Laravel 13 / PHP 8.4, авторизация, пользователи и RBAC, настройки с зашифрованными секретами, аудит; backend проектов, задач и календаря.
- Media Desktop: 19 программ, окна и Snap Layouts, сохранение персонального расположения, четыре approved-набора значков, общие настройки оформления и отдельные профили пользователей. Einstellungen — native-интерфейс с локализованными редакционными текстами и управлением пользователями. Live Studio подключён к desktop-окну через JSON API: список, создание, редактирование, публикация, OBS-вход, ротация ключа и получение RTMPS-данных больше не требуют отдельной HTML-ссылки.
- Media Library: цельные материалы и отдельная файловая галерея; поиск, фильтры, protected preview/download с HTTP Range, resumable upload папок/файлов с pause/resume/stop, теги, коллекции, редактирование плейлистов и массовые действия. Удаление материалов обратимое; замена cover/video/attachments не перезаписывает originals и чужие связи. Проверка квоты resumable upload совместима с MariaDB: зарезервированные байты считаются без сырого выражения с колонкой `offset`.
- Import Center: серверные папки/архивы, загрузка архивов до 20 GB, существующие intake/YouTube archives и публичные ссылки через collector/yt-dlp. Фоновая очередь, stop/retry, checkpoints, отдельное окно Import-Vorgänge, пообъектный отчёт и индикация стадии/файла/байтов/давности сообщения. Активность процесса очереди не выдаётся за подтверждение завершения конкретного импорта.
- Publishing: Website немедленно запускает выбранные внешние действия без подтверждения. Реальные YouTube/Facebook/Instagram/Telegram/X connectors; OAuth YouTube/X, encrypted credentials/RTMP outputs и resumable YouTube chunks с восстановлением ID после потерянного ответа. Processing checks идут через общую очередь; пять consecutive failures, pending не исчерпывает бюджет. Beiträge с видео загружают оригинал; текст/изображение/аудио адаптируются ffmpeg там, где нужен видеоформат, через текущий public/media/SHA-256 contract без замены оригиналов. Live: один reusable YouTube key и несколько независимых foreground RTMP outputs, public lifecycle confirmation YouTube, Live edits не прерывают relay. Reverse YouTube import раз в сутки за 24 часа + завершение уже обнаруженных items, только Review, без отправки обратно. Website edits/hide/delete распространяются только по поддерживаемым API; irreversible deletion требует explicit/opt-in политики, выключенной по умолчанию. Подробные ограничения: docs/PUBLISHING.md; production/API/encoding/streaming не проверены.
- Takeout: единая распакованная папка private/Takeout либо multipart ZIP; CSV задают IDs, тексты и связи, HTML archive_browser.html проверяет файловый состав. При отсутствии отчёта сохранён fallback с предупреждением. Импортируются видео, Beiträge, изображения, опросы/Quiz, комментарии, Livechats, плейлисты и контоданные; неизвестные схемы остаются в исходной выгрузке и перечисляются в отчёте.
- Повторные импорты объединяют записи по source/ID и originals по SHA-256/размеру/MIME; дополняют metadata/assets, сохраняют ручные правки, исходные версии, trash и исключённые связи. Обложки/комментарии/playlist positions связываются по достоверным IDs и metadata; неоднозначности остаются в отчёте, а не угадываются.
- Lokale Videos prüfen: серверный отчёт по registered originals, размерам и ffprobe; отдельная браузерная проверка same-origin HTTP Range, декодированного кадра, seek и короткого воспроизведения с сохранённым результатом.
- ИИ-разметка импорта: экономная обработка имеющихся текстов/метаданных/готовых субтитров и небольших изображений; журналы, snapshots и защищённая отмена. Takeout не запускает автоматические платные ИИ-задания и не превращает отдельные картинки в самостоятельные Beiträge.
- Import Center имеет сохраняемые настройки назначения по длительности: enabled, min/max секунд, Beiträge/Videos/Shorts. Manna получает первоначальное правило 14–16 → Beiträge; настройки, включая отключение, имеют приоритет. Импорт автоматически назначает подходящие unsorted video/short записи без ИИ, сохраняя файлы/комментарии/плейлисты; ready/manual/ai/trash защищены. ИИ получает то же текущее правило как доверенную подсказку. `.env` не меняется.
- Старый /upload/ отключён: страница перенаправляет в Desktop, API возвращает 410; приватный intake archive сохранён, прежний интерфейс доступен в Git.
- Public Website: все 10 approved-страниц — главная, Videos/detail, Beiträge/detail, Bücher/detail, Live, Podcast, Community. Каталоги, детали, поиск, фильтры, пагинация, вкладки и локальные плееры читают БД; пустые блоки видимы с обозначением отсутствия данных. Live-плеер передаёт MediaMTX cookie-check внутри `/_live/`, а nginx-инструкция явно переписывает служебные редиректы обратно в этот префикс. Если Live-событие завершено или HLS недоступен, плеер показывает афишу/брендированный fallback вместо чёрного видео и сетевой ошибки; для следующего события выводятся его cover, название, дата и описание. В публичном Live-блоке технические подписи заменены данными события, напоминания вынесены в компактные цветные кнопки над плеером без Email/Push-подсказок, а чат доступен гостям и зарегистрированным пользователям, показывает отправителю результат проверки, использует увеличенную область сообщений и компактное временное уведомление. Главная выбирает hero-видео среди опубликованных Videos: отмеченные флажком «Auf der Startseite anzeigen» имеют приоритет; без отметок берётся самое новое видео. Livestream в этот выбор не попадает. Hero-фон и боковая надпись используют локальные градиенты и текстовые элементы approved-композиции; публичный логотип разделён на знак и текст. Public CSS/JS изолированы от Desktop.
- Public Video analytics: фактический просмотр регистрируется при запуске опубликованного локального видео, сессия учитывается не более одного раза в сутки для одного видео; блоки «Beliebte Videos» сортируются по этим записям, а не по неподтверждённому `metadata.views`. Просроченные запланированные Live-события при обращении к сайту или Live Studio переводятся в `ended` с причиной `missed_schedule`; в ближайшие эфиры попадают только будущие даты.
- Public account: `/konto` оформлен как полноценный адаптивный личный кабинет в Manna-стиле; активность, прогресс, browser reminders, подтверждение email и профиль/пароль показаны в отдельных карточках без сырого JSON и внутренних translation keys.
- Public account разделяет staff и обычных пользователей: аккаунты с правами управления пользователями/настройками не получают запрос подтверждения email; опубликованные медиа и загрузки остаются в Media Desktop, а `/konto` работает только с личными состояниями текущего пользователя.
- Staff-аккаунт в `/konto` не читает и не показывает activity, browser reminders или профиль/пароль; единственное действие — перейти в Media Desktop. Staff также не создаёт public content states через публичные реакции.
- Обычный `/konto` собирает личную историю из существующих данных: покупки по email, newsletter-подписки, сохранённый контент и прогресс, собственные Community/Kommentar/Livechat-сообщения, Buchrezensionen и вопросы AI-помощнику с переходами к исходным материалам.
- История сообщений, Buchrezensionen и вопросов AI-помощнику в `/konto` не обрезается искусственным лимитом: большие списки доступны постранично, а счётчики показывают общее количество записей.
- Ошибка Blade ParseError на `/konto` исправлена: тип сообщения вычисляется в контроллере, а не через фигурный `match` внутри Blade-выражения.
- Шаблон `/konto` дополнительно переписан в многострочную Blade-разметку с балансом всех условных блоков; устранён повторный `unexpected token endif` при компиляции кабинета.
- Исправлена синтаксическая ошибка в `public/card.blade.php`, из-за которой production Blade-компиляция выдавала `unexpected token "endif"` и страницы `/live` и `/videos/...` отвечали HTTP 500.
- Brand background: предоставленная владельцем панорама `manna-mountains.png` установлена как единый hero-фон главной и публичных разделов, а также стандартные горные обои Desktop; на главной approved-композиция `01-start` получила overlay-шапку и измеренную высоту hero около 400 px; прежний временный фон больше не используется в этих местах.
- Über uns, Mission и юридические тексты редактируются в настройках. Контактная форма сохраняет обращения в защищённый inbox и пересылает через очередь на settings.contact_email; повторные попытки, видимый статус и ручной retry. Email Live-напоминания поддерживают PHP/sendmail Plesk без обязательного внешнего SMTP.
- Web Push Live: согласие и разрешение браузера, подписка/отмена на событие, encrypted subscriptions, приватные стабильные VAPID keys, scheduler и retryable jobs с проверками публикации/времени. Поддержаны ограниченные endpoints служб доставки Google/Mozilla/Apple/Microsoft.
- Upload-профили: отдельные интерфейсы загрузки сохраняются в каждом разделе, а общий resumable-механизм проверяет профиль назначения. Изображения для Media Library, Live-афиши, Cover, аватара и обоев автоматически оптимизируются в браузере; сервер повторно проверяет MIME, размеры и итоговый объём. Видео, архивы и вложения не перекодируются.
- Live: обновляемый чат и счётчик активных browser sessions. Гости и зарегистрированные пользователи отправляют сообщения без авторизации и лимитов; сообщения проходят фоновую AI-проверку, безопасные публикуются автоматически, спорные попадают в отдельную очередь человека. Модерация находится только в защищённом Community-модуле Media Desktop; публичная страница не содержит управления очередью. Опубликованный чат обновляется через heartbeat. Live Studio — единственный административный интерфейс вещания и работает через JSON API внутри `/desktop`; отдельный legacy `/desktop/live` HTML-интерфейс удалён. Для события в Live Studio можно загрузить одно защищённое изображение-афишу через существующую Media-библиотеку; оно связывается как `cover_media_id` и используется публичными Live-карточками/анонсом. Загрузка афиши теперь показывает отдельные состояния выбора, подготовки, процента передачи, привязки, успеха и ошибки с возможностью повторного сохранения. Будущие события не попадают в «Aufzeichnungen»: записи отделены фильтром `live_status=ended`, а Live Studio сортирует текущие/запланированные события выше завершённых.
- OBS → MediaMTX → локальный плеер: защищённое управление событиями и encrypted keys, общий RTMPS ingest path/key для всех событий, выбор enabled-события по live status и расписанию, ingest/HLS authorization, pinned installer/start/status, private recording hooks и регистрация MP4. Новый заголовок не требует менять OBS; локальный RTMP/SSH остаётся административным fallback, зрители — через nginx /_live/. В Live Studio показывается одна общая RTMPS-адреса, без SSH-инструкций для стримера. Сертификат/ключ и firewall остаются одноразовой серверной настройкой.
- Посетители: регистрация без административной роли, email verification, личный /konto с собственными реакциями/закладками/прогрессом/напоминаниями, отменой подписок и изменением профиля; восстановление пароля через existing Laravel broker и encrypted queue.
- Newsletter: double opt-in, queued confirmation, подписанные ссылки подтверждения/отмены, согласие, cooldown и статусы доставки.
- Книги: защищённый редактор metadata/цены/статуса; verified читатели отправляют отзывы, изменения требуют повторной модерации. Community: вопросы/обсуждения посетителей, правила из настроек, защищённая publish/reject модерация с аудитом.
- Public header: все публичные страницы используют единую оболочку header; пустая верхняя полоса Bücher удалена, кнопка Mitmachen получила безопасный отступ и не обрезает иконку, Live получил согласованный фон страницы.
- Public UI shell: один переиспользуемый overview-header/hero используется главной и всеми разделами, включая Live; один общий detail-header используется страницами отдельных видео, Beiträge, книг и выбранных Live-событий, а публикационные данные меняются через содержимое страницы. Live-плеер и чат выводятся только для выбранного события; служебные overview/about-страницы используют локализованный общий hero без сырых translation keys. Hero теперь имеет один общий desktop/mobile-каркас и геометрию для всех overview-страниц; отсутствующее featured-медиа получает брендированную fallback-заглушку, а fallback URL вычисляется до разметки.

## Текущее состояние и решения

- Community moderation: гости и зарегистрированные пользователи могут отправлять community-посты, комментарии и Livechat без авторизации и частотных лимитов. Автор сразу видит собственное сообщение в состоянии проверки; остальные видят его только после AI-разрешения. AI-подозрительные сообщения остаются у автора красными как «заблокировано» и попадают в human-review очередь; после трёх AI-блокировок отправка новых сообщений запрещается. Управление очередью вынесено в защищённое окно Community внутри Media Desktop; публичный сайт не показывает административные действия.

- Self-hosted single-tenant; Manna Vom Himmel — первая установка, не отдельный Core. Основной UI немецкий, новые строки локализованы.
- Publishing decision: публикация Website остаётся немедленной и запускает внешние Jobs без дополнительного подтверждения; обратная синхронизация YouTube всегда создаёт `review`/`public_published=false`. `manna-youtube-runtime` не используется как основа новой интеграции; для review-импорта предусмотрен отдельный `yt-dlp`-процесс.
- Blade и progressive JavaScript без обязательной Node-сборки. Composer используется для PHP-зависимостей Laravel, включая minishlink/web-push; не нужен при обычном обновлении текстов/Blade.
- Production checkout: /var/www/vhosts/mannavomhimmel.de/httpdocs; document root: httpdocs/platform/public; runtime: private/atapin-platform. PHP: /opt/plesk/php/8.4/bin/php. Queue/scheduler — Plesk scheduled tasks, без самостоятельной установки systemd.
- MediaMTX после reboot запускается отдельной Plesk command task каждую минуту; lock предотвращает второй процесс. RTMPS использует файлы private/atapin-live/rtmps.crt и rtmps.key, не входящие в Git.
- Предпочтительный Takeout источник: /var/www/vhosts/mannavomhimmel.de/private/Takeout; TAKEOUT_FOLDER позволяет переопределить путь. Takeout и `.env` сохраняются полностью. Владелец сообщил о полном сбросе БД и создании нового администратора; private/manna-youtube удалён владельцем, runtime сборщика сохранён. Новых удалений БД/серверных файлов агент не выполнял.
- Новые бесплатные видео, audio/podcasts, изображения и субтитры физически сохраняются в /var/www/vhosts/mannavomhimmel.de/httpdocs/platform/public/media с именем SHA-256 содержимого. Admin/public UI получают прямые static URLs `/media/<hash>.<ext>`, без симлинков и PHP-передачи бинарных файлов. Takeout импортируется по одному полному объекту: bytes готовятся до короткой транзакции metadata/assets/comments/playlist links. Исходный экспорт сохраняется; retry/checkpoints не создают дубликаты. Подробнее: docs/MEDIA-STORAGE.md.
- Публикация материалов не автоматическая; только published ready records/children видны на сайте, архивные контоданные исключены из public и ИИ. По решению владельца бесплатные бинарные медиа публично адресуемы независимо от редакционного статуса. Документы, архивы и staging остаются private. Платные video/audio потребуют отдельного защищённого storage/выдачи с удалением публичной копии, не просто флажка на странице.
- Видео/файлы публикуются с нашего сервера, без YouTube fallback/кнопок перехода. Адреса источников остаются provenance; приватные originals не раскрываются.
- Платное прослушивание/анализ audio/video не входит в текущий план. Реальные production-письма, Push и вещание проверяет владелец; отсутствие этих проверок у агента не считается недоработкой реализации.
- Владелец подтвердил, что новый импорт ещё не запускался и каталог пустой. Правило длительности применяется автоматически при новом импорте, не публикует материалы и не запускает платный ИИ. Publishing исправлен отдельным commit `1b0cae2`; его изменения не входят в блок импорта/public tests. Секреты и клиентские данные не входят в Git.

## Известные ограничения

- На ширине 820 px верхнее desktop-меню главной выходит за viewport; существующие header-стили в исправлении боковой надписи не менялись. Требуется отдельная корректировка breakpoint меню.
- 100% pixel match не подтверждён; пустые блоки пока намеренно включены.
- Оплачиваемый checkout не реализован: требуется выбор способа оплаты владельцем. Покупка пока через контакт; платные PDF не выдаются публично.
- Newsletter campaign editor и массовая рассылка не реализованы; подписка/подтверждение/отмена реализованы.
- Записи Live разбиваются на сегменты; объединённый replay отсутствует, завершённый эфир воспроизводит последний зарегистрированный сегмент, а при его отсутствии показывает афишу. Автозапуск MediaMTX требует одноразовой настройки Plesk command task, общий RTMPS-ключ нужно менять только вручную через checkbox ротации, RTMPS — установки публичного сертификата и открытия TCP 1936 владельцем.
- Полные native Desktop-интерфейсы проектов, задач и календаря ещё отсутствуют; Shop имеет редактор товаров/отзывов, но не полный интерфейс продаж. Podcast и Themen/Kategorien не имеют отдельных backend-модулей. Ярлык или безопасная конфигурация интеграции не означает готовый модуль/OAuth/публикацию во внешнюю службу. Browser-native вещание исследовано, но не внедрено: для базовой камеры/микрофона возможен WebRTC/WHIP, а полноценный OBS-подобный микшер источников потребует отдельной клиентской media-архитектуры.
- Takeout проверен на немецкой схеме владельца и основных английских aliases, не на всех возможных языках/форматах. Отдельного достоверного Short-признака экспорт не гарантирует: прежние Shorts сохраняются, новые неопределённые видео требуют review.
- Checkpoints сохраняются между единицами работы с мягким бюджетом 30 секунд; большой ZIP entry/hash/предварительная индексация могут выполняться дольше. Это не побайтовое возобновление ZIP. Старые дубли originals физически не удаляются.
- ffprobe нужен для серверной проверки формата; наличие файла/metadata не доказывает браузерное воспроизведение.
- Перед полным Takeout import интерфейс не выполняет исчерпывающий readiness-check: platform:check проверяет SQL/private storage/Owner и счётчики jobs, но не public/media write access, весь запас места или фактическое обслуживание очереди. Для распакованного Takeout источник сохраняется: требуется дополнительное место под новые media (~56 GB плюс configured reserve 5 GB по умолчанию); ZIP также требует extraction space. Это проверяется перед первым запуском, не постоянным ручным надзором.
- Шесть старых падений Foundation/PublicAccount/PublicPages устранены: повторное сохранение профиля больше не создаёт duplicate user_profiles; тесты проверяют видимый DOM вместо JSON-переводов, текущий гостевой чат, стабильную идентичность посетителя и pre-worker состояние модерации. Import Center upload/history/undo/settings desktop/mobile прошёл. Остался ранее найденный mobile overflow книги `/buecher/vorschau`, не исправлявшийся этим блоком; общий public browser suite не повторялся. Массовый импорт 56 GB и реальные внешние API/Live не запускались.
- Publishing: для YouTube public нужен разрешённый API project/Google audit; X требует OAuth app/write-media permissions/credits, Meta и Telegram — разрешённые аккаунты/tokens. TikTok unattended Direct Post несовместим с правилами private utility; YouTube Community заменяется видеоадаптацией, не native post. Generic REST/API connectors не реализованы, RTMP требует выданный платформой ingest key и не подтверждает remote playback. X metadata edit и Instagram edit/hide/delete не реализованы; обычное Website unpublish не уничтожает неподдерживаемые private-копии. Lost creation responses вне resumable/recoverable API всё ещё могут дать дубли; expired YouTube sessions не перезапускаются автоматически. Reverse sync не backfill'ит пропущенные дни и не распространяет изменения/удаления YouTube→Website. Исторические дубли не удаляются. Native Community/Books/прочие отдельные модели не подключены к этому outbound SourceRecord workflow.

## Рекомендуемый следующий этап

- Согласованный план Telegram video links + Main Mini App (пока НЕ реализован): для новых видеоматериалов отправлять обложку, заголовок, имеющуюся Kurzbeschreibung и ссылку/кнопку вместо видеофайла. Зарегистрировать через BotFather Main Mini App существующего бота, открывающую текущий public Website с его плеером и нашими media; добавить минимальную SDK-адаптацию viewport/safe areas/Back без второй копии сайта. В channel posts использовать обычную URL-кнопку с Mini App deep link/startapp, проверенный material token вести только на опубликованную public detail-страницу; обычную Website-ссылку оставить fallback. Mini App сама не требует канала, экспорт в канал по-прежнему требует Chat-ID/прав бота. Секреты не раскрывать; identity actions только после server-side initData validation. Старые сообщения, native text/image/audio, другие платформы, login/payments/reverse import/webhooks не переделывать. Подробности: docs/PUBLISHING.md. Сейчас только план, без кода/API/регистрации; implementation после отдельной команды владельца.
- Согласованный план Social Media forms (пока НЕ реализован): проверить формы YouTube/Facebook/Instagram/Telegram/X/TikTok/LinkedIn по существующим connectors/OAuth и официальной документации; составить матрицу обязательных, необязательных, автоматически получаемых и лишних полей. Исправление затем должно охватить видимость/переключение/reload, provider-specific server validation, исключение нерелевантных полей из отправки, сохранение секретов и краткие de/en инструкции. Webhook Secret показывать только при работающем входящем workflow: локально управляемые секреты генерирует программа и хранит зашифрованно; provider-issued signing/app secrets не подменять генерацией. Telegram outbound требует Bot Token + Chat-ID, публичная URL необязательна, OAuth/webhook не нужны. Недоступные publishing connectors не выдавать за подключённые. Подробный план: docs/PUBLISHING.md; формы, credentials и API сейчас не изменять, реализация только после отдельной команды владельца.
- Согласованный план External image import (пока НЕ реализован): в следующих Takeout/архивных импортах внешние URL изображений, указанные в media-полях (включая канал/аватар/баннер), не считать готовым импортированным изображением. Скачать изображение в существующей фоновой очереди, сохранить через canonical media/public-media SHA-256 contract, зарегистрировать в Media и связать с соответствующим объектом; в интерфейсе использовать локальный файл. Повторный запуск не создаёт дубли. Обычные ссылки в тексте, рекламу/донаты и произвольные внешние страницы этим пунктом не скачивать. Применить безопасную сетевую загрузку с защитой от private/internal URL и небезопасных redirects, лимитами размера/времени и проверкой MIME изображения. Недоступная ссылка даёт понятную ошибку изображения и возможность повторить, а не «Lokale Videodatei fehlt»; отсутствие локального файла и успешное скачивание различать в отчёте. Это план будущих импортов: завершённый импорт/Takeout сейчас не изменяются, существующие remote-asset warnings автоматически не удаляются.
- Согласованный план Podcast import (пока НЕ реализован): добавить выбор «Podcast» при импорте и возможность пакетно назначать выбранные материалы подкастами. В существующей фоновой очереди извлекать звуковую дорожку из локального видео через ffmpeg без ИИ и без перекодирования при совместимом формате; при несовместимом формате потребуется преобразование аудио. Сохранять отдельный audio-файл через canonical public/media/SHA-256 contract и показывать материал в разделе Podcast с обложкой и аудиоплеером, без видеоплеера и передачи тяжёлого видео для прослушивания. Заголовок, описание, обложку и связанные комментарии сохранять; исходный Takeout не изменять, не удалять видео автоматически. Длительность 4–5 минут сама по себе не назначает Podcast: выбор пользователя имеет приоритет, распознавание неподвижной картинки — отдельная будущая проверка, не готовая функция. Другие правила назначения, включая 14–16 секунд → Beiträge, не заменять этим наблюдением.
- Согласованный план Featured Video (пока НЕ реализован): тонкая белая рамка со скруглением как в approved-образце; фотография и имя автора внизу карточки из настроек проекта, с загрузкой фото и аккуратным аватаром-заглушкой при его отсутствии. Заметная круглая Play-кнопка с белым контуром ближе к центру композиции вместо маленького треугольника в нижнем углу. Заголовок и Kurzbeschreibung должны читаться поверх cover: при отсутствии свободного места нужна локальная полупрозрачная подложка под текстом, не overlay на всю картинку. При отсутствии cover используется извлечённый кадр видео по плану Public Video ниже. Имена/фото/счётчики из образца не переносить как реальные данные. Проверить desktop/mobile без наложений; программирование остаётся на паузе до завершения соседней работы.
- Согласованный план Hero-Sprüche (пока НЕ реализован): добавить в существующую админку сайта редактирование рукописных христианских фраз отдельно для главной и каждого overview-раздела, с сохранением текущих фраз как defaults и поддержкой локализации. Пользователь просил сейчас только записать этот план; интерфейс редактора и хранение пользовательских текстов сделать отдельной будущей задачей.
- Согласованный план Live после окончания эфира (пока НЕ реализован): при переходе события в `ended` публичный плеер должен сразу переключаться на штатный fallback с обложкой и статусом `Beendet`, не показывая сначала чёрный экран, ошибку HLS/manifest `401` и бесконечные повторы. Ответы служебного Live-маршрута не должны запускать нативное Basic-Authentifizierungsfenster браузера; клиент должен прекращать запросы к завершённому эфиру, а серверный public lifecycle и fallback должны оставаться согласованными. Обложка и плеер должны использовать подходящую стандартную геометрию `16:9`, чтобы обложка полностью помещалась без непредусмотренных боковых полос и без обрезания важного содержимого. Проверить desktop, узкую desktop-ширину и mobile; текущая ошибка зафиксирована владельцем на `/live?event=13466`.
- Согласованный следующий блок Public Video (пока НЕ реализован; программирование приостановлено до завершения соседних изменений Publishing/public UI):
  - Не показывать публичные кнопки/ссылки «скачать видео», включая материалы вебинаров. Это изменение интерфейса, не запрет самостоятельного сохранения напрямую отдаваемого MP4; admin download сохраняется. Удаление отдельных текстовых/PDF/субтитровых материалов этим пунктом не согласовано.
  - Комментарии и реакции отправлять без перезагрузки страницы. Кнопка отправки запускает запрос; краткое состояние отправки/успех/ошибка показывается возле неё, не общим баннером вверху сайта. Не допускать повторного клика во время запроса; при ошибке сохранять текст. После успешной отправки комментарий виден автору в состоянии проверки.
  - Комментарии видео используют ту же существующую ИИ-модерацию и human-review логику, что Live-чат: собственный pending виден автору, другим — только после разрешения. Не создавать второй модератор; проверить обновление результата без reload.
  - Добавить отдельную редактируемую Kurzbeschreibung: 8–10 немецких слов по заголовку/описанию, без рекламы книг, пожертвований, контактов, ссылок, хештегов и призывов к активности. При недостаточной информации не выдумывать тему. Оригинальное описание сохраняется; сверху страницы/в карточках показывается короткий текст, полное описание компактно свёрнуто и раскрывается кнопкой «Mehr anzeigen» с возможностью свернуть обратно.
  - Пакетная генерация Kurzbeschreibung: выбранные видео либо все без короткого текста, до 100 объектов на запрос с дополнительным ограничением по объёму входа/выхода, результат связан по ID, проверяется число слов, ручные тексты не перезаписываются. Использовать существующую очередь/ИИ-настройки; 100 — верхняя граница группы, не обещание вместить любые 100 описаний.
  - Обложки: при отсутствии cover автоматически извлекать кадр из локального видео, без ИИ-расходов; существующие cover не заменять. Отдельная кнопка генерации ИИ по проекту/заголовку/описанию, общий стилевой prompt проекта для единой серии. Сохранять через текущий canonical media contract, не менять прямую выдачу видео.
- Обновить код, проверить настройки длительности Import Center и запустить новый Takeout import. Проверить отчёт и первое настоящее большое MP4/аудио: маленький MP4 уже проверен на production nginx, но не доказывает скорость всех файлов Takeout. Исходный Takeout не переносить вручную. Временный playback-check-20260914.mp4 после проверки удалить владельцу точной командой; остальные файлы не затрагивать.
- Применить Publishing update: git pull, config:clear/view:clear; route:clear при ранее кешированных routes. Уже установленные Live hooks используют тот же entrypoint; public:live-config нужен вне эфира только если предыдущие foreground hooks ещё не применялись. Подключить OAuth/платформы, проверить ffmpeg codecs/font, настоящий upload/processing/Live/ежедневный Review import. Initial Publishing migration должна быть ранее применена с backup; новых migrations/dependencies/cron tasks этот блок не добавляет.
- Для диагностики Takeout использовать Ergebnis im Detail → связи/ошибки/манифест и Lokale Videos prüfen → Alle im Browser prüfen; не очищать originals и не заменять проверку ручным просмотром сотен записей.
- Инструкции стримера и администратора доступны непосредственно в Live Studio внутри `/desktop`; установка MediaMTX/nginx, сертификата/порта RTMPS и production-проверки выполняются владельцем.

## Проверки

Локальные проверки, кроме отдельно указанного read-only nginx-теста.

- План Telegram video links + Mini App: сверены официальные launch/deep-link/button/data-validation contracts, просмотрен документационный diff, git diff --check прошёл. Тесты, сборки, account API и production не запускались; функции не реализованы и бот не зарегистрирован как Mini App.
- План Social Media forms: прочитаны текущие provider templates, shared validation, Telegram connector и документация Telegram webhook; проверен документационный diff и git diff --check. Тесты, сборки, внешние account API и production не запускались; исправление форм не реализовано.
- Светлые hero-подложки и разные фразы разделов: 28 целевых Feature tests / 377 assertions passed, включая de/en, уникальность фраз и отсутствие надписи на detail-страницах. Edge проверил девять overview-страниц с пустыми и заполненными данными при ширинах 1672, 1280, 390: локальный белый gradient, blur, отсутствие наложения на медиа и overflow, assets/JS. Просмотрены desktop/mobile screenshots. PHP/JS syntax, Blade `view:cache` и `git diff --check` passed. Админ-редактор не реализован; план сохранён. Production не обновлялся.
- Перенос боковой надписи: 27 целевых Feature tests / 297 assertions, Blade `view:cache`, JS syntax и `git diff --check` passed. Edge: девять overview-страниц при 1672×941/390×844 с пустыми и заполненными данными; главная дополнительно при ширинах 1920, 1601, 1600, 1590, 1280, 1024, 800, 390. Проверены отсутствие наложения надписи на медиа, безопасный отступ, assets/JS и overflow; просмотрены desktop/mobile screenshots и approved UI. Отдельная проверка 820 px обнаружила overflow существующего меню, не изменявшегося этим блоком. Production не обновлялся.
- Удаление декоративных стрелок: 26 целевых Feature tests / 293 assertions, Blade `view:cache`, JS syntax и `git diff --check` passed. Edge проверил девять overview-страниц desktop/mobile на пустой и заполненной изолированной SQLite fixture; отсутствие отмеченных стрелок на главной/Bücher, assets/JS и overflow проверены. Просмотрены снимки; измерено неизменное наложение правой надписи. Production не обновлялся.
- Переводы назначения по длительности: Import Center browser workflow на Edge passed с отдельной проверкой всех трёх подписей options. `node --check` browser test и `git diff --check` passed. Production и полный suite для этой одноточечной Blade-правки не запускались.
- Компактный Takeout UI: существующий Import Center browser workflow на Edge passed, включая короткую карточку, отсутствие подробного текста в рабочей форме и открытие инструкции через «?». PHP syntax четырёх translation files, JS syntax browser test и git diff --check passed. Production/массовый Takeout import не запускались.
- Desktop window browser на Edge passed: Community deeplink → закрытие → reload → logout/login, восстановление открытого окна, Close all, новый deeplink Videos, неизвестный app ID, сохранение других query/hash и отсутствие JS page errors. Проверка добавлена в CI. `node --check` desktop-os.js/desktop-window-browser.mjs и `git diff --check` passed. Production и полный Laravel suite в этом узком JS-блоке не запускались.
- Read-only review готовности импорта: повторно просмотрены UI→queue→checkpoints→целый объект→canonical media и автоматическое правило длительности. 6 лёгких tests / 56 assertions passed за 0.85 sec (ImportClassificationRulesTest и полный fixture Takeout→Beiträge). Блокирующих ошибок в этом пути не обнаружено; production permissions/free space/queue и полный 56 GB не проверялись. Запуск уже есть в Import Center: Google Takeout vom Server → Bereits entpackter Export: Takeout → Import starten; длительность предварительно сохранить 14–16 → Beiträge, основной target оставить mixed. Переносить Takeout вручную не нужно.

- Этот блок: полный Laravel SQLite через PHP 8.4.25 — 220 tests / 1636 assertions passed на совместном дереве; после добавления регрессий — 62 целевых / 605 assertions и 12 CatalogPlayback/TakeoutOriginalAccess / 66 assertions. Проверены настройки/валидация/RBAC, границы длительности, полный объект Takeout→Beiträge, сохранение ручных правок, гостевые session views и прямой URL audit-отчёта. MySQL локально не запускался; остаётся CI-проверкой.
- Import Center browser на Edge прошёл: настоящее PNG canvas вместо повреждённой fixture, устранено перекрытие панелей пагинацией, загрузка/назначение/undo/replacement/trash/playlist, настройки после reload, desktop/mobile. Import workflow browser прошёл на реальном сгенерированном WebM с byte-accurate Range fixture-транспортом (PHP development server не поддерживает Range для static), включая decoded frame/seek/play и сохранённый результат. Это не имитация production-проверки — nginx проверен отдельно.
- Production read-only: созданный владельцем MP4 `/media/playback-check-20260914.mp4`, 394963 bytes / 4 sec. nginx HEAD 200 video/mp4 + Accept-Ranges, начало/конец 206 с корректными Content-Range. Edge: decoded 640×360, старт 310 ms, seek к 3 sec и продолжение passed; 3 ответа 206, без redirect. Серверные настройки/БД/Takeout агент не изменял, большой архив не импортировал.
- Повторный static-media-browser.mjs: настоящий MP4 /media/<sha256>.mp4, decoded/play, без redirect/overflow desktop 1672×941 и mobile 390px. PHP/JS syntax, Blade view:cache, route:cache и upload pause/resume/stop harness passed; git diff --check passed.
- Universal overview header/hero: панель логотипа/меню ограничена по ширине и имеет одинаковые скругления, включая Live. Общий белый overlay удалён; сохранена только локальная полупрозрачная подложка с плавными краями за текстом. Панорама выровнена по верхнему краю. Исправлен вывод сырого `@include` вместо заглушки из-за соседних Blade-директив; версия CSS обновлена для браузерного кеша.
- Проверки overview header/hero: PHP 8.4.25 из `.local/php84/php.exe`, 28 целевых Feature tests / 292 assertions и Blade `view:cache` прошли. Edge проверил девять overview-страниц с пустыми и заполненными данными при 1672×941 и 390×844: геометрию панели, отсутствие общего overlay, верхнее выравнивание фона, локальную подложку, assets/JS и отсутствие overflow. Просмотрены desktop/mobile screenshots; отдельный home browser проверил меню и поиск. JS syntax и `git diff --check` прошли. Production этим блоком не обновлялся.

- Catalog classification rules: 41 целевой test / 326 assertions пройдены (правила, AI evidence, record/media classification, undo/merge и Takeout). Проверены числовая длительность, наследование без чтения видео, неизвестные/противоречивые значения и изоляция правила Manna. HTTP провайдера mocked; платные ИИ-вызовы не выполнялись. PHP syntax и `git diff --check` пройдены.
- Public media/imports: PHP 8.4.25 из `.local/php84/php.exe`; 65 целевых tests / 470 assertions пройдены. Покрыты полное завершение объекта до следующего файла, retry после отказа БД, очистка незавершённой копии, прямые video/audio/cover URLs, дедупликация и приватность документов. Старые заявления об отсутствии локального PHP ошибочны.
- Blade `view:cache`, JS syntax и upload pause/resume/stop harness пройдены.
- `static-media-browser.mjs` на Edge создал настоящий MP4: прямой `/media/<sha256>.mp4`, без redirect, decoded frame/playback и отсутствие overflow при 1672×941 / 390px. Просмотрены screenshots обоих размеров. PHP development server не доказывает nginx Range.
- Параллельная Publishing-задача также прогнала полный composer test: 224 tests / 1650 assertions passed на совместном дереве; это её отдельный подтверждённый результат, не повторный прогон данного блока. Общий public browser suite не повторялся.
- Public AJAX: 33 целевых Laravel tests / 252 assertions passed (PublicCommunity, PublicPages, PublicNewsletter, PublicContact), включая JSON live-chat submission and no-reload marker; PHP syntax checks for changed controllers and `node --check public.js` passed. Browser/production playback checks were not rerun for this focused request.
- Ранее read-only проверены реальные Takeout CSV и состав 1 815 файлов / 244 видео без массовой распаковки; это не результат нового полного импорта 56 GB.
- Publishing: в предыдущем implementation-блоке `9ad5649` полный composer test на PHP 8.4.25 / SQLite — 269 tests / 1991 assertions passed; Edge publishing-browser, PHP/JS syntax, Blade view:cache, route:cache и git diff --check прошли. HTTP/Process mocked; настоящие ffmpeg encoding/MediaMTX/platform publishing/OAuth/production и MySQL локально не запускались. Текущие четыре исправления проверены чтением кода и diff: YouTube DELETE передаёт ID в query без тела; GUI восстанавливает сохранённые назначения, включая пустой выбор; Website показывает unpublished после снятия/удаления; Telegram сохраняет тип отправленного сообщения вместе с ID. Регрессионные проверки обновлены, но тесты, сборки, syntax checks и внешние API по прямому запрету владельца не запускались. git diff --check прошёл; прежние результаты тестов не относятся к текущим правкам.

## Последний связанный commit

- Текущий документационный блок: Record Telegram video links and Mini App plan; предыдущий связанный план форм — `bdbcf24`, implementation commit — `ccaac74`. План и статус включены в один commit; hash сообщается после commit/push. Ветка/upstream: main → origin/main. Deployment не требуется.
- Publishing: текущий atomic-блок Fix publishing destinations and publication state; предыдущий связанный commit `9ad5649` — Complete automated media publishing workflow. Изменения и этот статус включены в один commit, hash сообщается после commit/push. Ветка/upstream: main → origin/main; production не обновлялся.
- Последние документационные требования сохранены в `9f0edc8` — Record agreed public video follow-up plan, `8a87bda` — Record featured video presentation plan и `83237ee` — Record audio-only podcast import plan. Текущий документационный блок: Record external image import plan; добавлено скачивание внешних изображений в локальные media при будущих импортах и понятные ошибки изображений. Функции не объявлены реализованными. Последний связанный implementation commit: `ccaac74`. Deployment для записи плана не требуется.
- Предыдущие связанные commits: `e40cb5a` — Serve physical public media and import complete Takeout objects; `7ca7003` — Add Manna fifteen-second post classification guidance; `1b0cae2` — Fix publication retries and live relay.
- Текущая ветка и upstream: main → origin/main.
- Последние связанные commits: `2ed3195` — Configure automatic import sorting and pass pre-import checks; `704329a` — Unify public overview hero layout; `79de793` — Document import readiness review; `223faf0` — Consume desktop deep links after opening apps; `f2309a3` — Move Takeout instructions into desktop help. Текущий блок: Fix translated import duration destinations; deployment требует получения кода и view:clear, без migration/изменения .env/очередей.
- Последний связанный public commit: `3bec710` — Move homepage hero quote outside featured media. Текущий atomic-блок: Brighten hero text clouds and add section encouragements; deployment требует только получения кода и Blade `view:clear`.
- Текущий public atomic-блок: Fix public AJAX forms and live-chat reload; commit/push выполнены в этой задаче. Deployment: получить platform code и выполнить Blade `view:clear`.

